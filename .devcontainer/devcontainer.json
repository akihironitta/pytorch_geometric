{
  "image": "mcr.microsoft.com/devcontainers/python:3.10",
  "postCreateCommand": "sudo apt update && sudo apt install cmake -y && pip install -U pip && pip install torch && pip install -e '.[full,test]'"
}